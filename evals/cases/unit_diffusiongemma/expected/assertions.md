# Expected: unit_diffusiongemma

- The agent's trajectory conforms strictly to the workflow described in `AGENTS.md`, `$keep-up-with` `SKILL.md`, and related references. The flow is effective and efficient.
- A deep dive is done and a thread is published to the most appropriate channel, expected to be `ai.models`.
- Explain DiffusionGemma clearly enough that the user understands the release, speed/quality tradeoff, local-serving target, and why text diffusion changes the generation loop.
- Incorporate enough high-value DiffusionGemma images in the thread to cover the release and technical blog. Expect at least 3 useful images from `expected/assets/`.
- Visual priority: high priority is the architecture image, latency chart, and benchmark comparison. Medium priority is the Sudoku comparison if the thread discusses fine-tuning. The hero image is optional.
- Include public discussion when it changes the read, especially HN debate around quality loss, cloud economics, and tooling behavior.
- Visuals should be cropped tightly to exactly where they need to be and readable, with no clipped labels, adjacent prose, browser chrome, or loose horizontal framing. See `expected/assets/` for examples of good crop quality.
- Quotes should put the quote first, then add compact source context directly under it. For public reactions, include point-in-time scale such as Reddit score/upvotes or X likes, reposts, and impressions. For official source quotes, name the source instead.
- Use `expected/reference.md` as the quality bar. The final thread should be on par with or better than that reference in coverage, visual use, plain-English explanation, source attribution, and readability.
