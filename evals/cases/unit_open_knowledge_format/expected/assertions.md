# Expected: unit_open_knowledge_format

- The agent's trajectory conforms strictly to the workflow described in `AGENTS.md`, `$keep-up-with` `SKILL.md`, and related references. The flow is effective and efficient.
- A short deep dive is done and a short thread is published to the most appropriate channel, expected to be `ai.tools`.
- Explain OKF as Obsidian-compatible Markdown plus a tiny agreed schema and packaging convention for machine exchange, with enough detail to make clear what the standard adds. Include that `type` is required but semantic/freeform, not a central enum, with examples such as `BigQuery Table`, `API Endpoint`, `Metric`, or `Playbook`; consumers are expected to handle unknown types gracefully.
- Use both source screenshots from `expected/assets/`: the folder layout image and the concept-file/frontmatter image. The thread should explain what each image clarifies.
- Visuals should be cropped tightly to exactly where they need to be and readable, with no clipped labels, adjacent prose, browser chrome, or loose horizontal framing. See `expected/assets/` for examples of good crop quality.
- Use `expected/reference.md` as the quality bar. The final thread should be on par with or better than that reference in coverage, visual use, plain-English explanation, source attribution, and readability.
