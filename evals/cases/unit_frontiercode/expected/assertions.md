# Expected: unit_frontiercode

- The agent's trajectory conforms strictly to the workflow described in `AGENTS.md`, `$keep-up-with` `SKILL.md`, and related references. The flow is effective and efficient.
- A deep dive is done and a thread is published to the most appropriate channel, expected to be `ai.evals`.
- Explain FrontierCode clearly enough that the user understands the launch, the mergeability target, the maintainer-review rubric, the main score picture, and the closed-task caveat.
- Incorporate enough high-value source visuals to cover the benchmark and blog. Expect at least 4 useful visuals from `expected/assets/`.
- Visual priority: high priority is the Diamond score, rubric/example task, score by dataset, and misclassification/error-rate comparison. Medium priority is task shape. Use public-reaction visuals only when covering public discussion.
- Include public discussion when it changes the read, especially X attention, swyx/METR framing, and r/codex skepticism about the Claude-heavy ranking and closed benchmark access.
- Quotes should put the quote first, then add compact source context directly under it. For public reactions, include point-in-time scale such as Reddit score/upvotes or X likes, reposts, and impressions. For official source quotes, name the source instead.
- Visuals should be readable and tied to the post they support, with no clipped labels, browser chrome, adjacent prose, or loose framing. See `expected/assets/` for the reference visual set.
- Use `expected/reference.md` as the quality bar. The final thread should be on par with or better than that reference in coverage, visual use, plain-English explanation, source attribution, and readability.
