# Expected: unit_iroh_1_0

- The agent's trajectory conforms strictly to the workflow described in `AGENTS.md`, `$keep-up-with` `SKILL.md`, and related references. The flow is effective and efficient.
- A concise message is published to the most appropriate channel, expected to be `ai.tools`. A short thread is acceptable if it stays similarly brief; a long deep-dive thread is not.
- The only message or first thread post must be high-level and intuitive: explain Iroh as a way for apps/devices to connect to each other without users dealing with IPs, ports, routers, or server setup.
- Mention when Iroh 1.0 shipped or started getting attention, briefly explain n0/Iroh if needed, and give a compact popularity signal.
- Do not cram networking implementation details into the only message or opener. Avoid dumping terms like QUIC, NAT traversal, relay fallback, public keys, crates downloads, or API details unless they are explained simply and are necessary.
- Include a relevant visual attachment, such as the repo/README surface, release page, or star-history chart. Visuals should be readable and tightly cropped.
- Use `expected/reference.md` as the quality bar. The final output should be on par with or better than that reference in plain-English explanation, visual use, source attribution, and readability.
