# Expected: unit_vercel_ai_sdk_harnesses

- The agent's trajectory conforms strictly to the workflow described in `AGENTS.md`, `$keep-up-with` `SKILL.md`, and related references. The flow is effective and efficient.
- A concise message is published to the most appropriate channel, expected to be `ai.tools`. A short thread is acceptable if it stays similarly brief; a long deep-dive thread is not.
- Explain Vercel's experimental `HarnessAgent` support clearly enough that the user understands the feature, its canary/experimental status, and the basic API shape.
- Use the provided `HarnessAgent` screenshot from `expected/assets/` and tie it directly to the explanation of the API shape.
- Visuals should be cropped tightly to exactly where they need to be and readable, with no clipped labels, adjacent prose, browser chrome, or loose horizontal framing. See `expected/assets/` for examples of good crop quality.
- Use `expected/reference.md` as the quality bar. The final message should be on par with or better than that reference in coverage, visual use, plain-English explanation, source attribution, and readability.
